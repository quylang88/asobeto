import type { LessonContent } from "../../types";
import { createPage2BubbleFloorLessons } from "../../page-helpers";

export const floor4Lessons: LessonContent[] = createPage2BubbleFloorLessons({
  lessonId: "w1p2-t4-f4-bubble-pop",
  targetLetters: ["ư", "t"],
});
