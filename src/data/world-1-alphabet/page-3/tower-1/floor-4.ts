import type { LessonContent } from "../../types";
import { createBubbleFloorLessons } from "../../page-helpers";

export const floor4Lessons: LessonContent[] = createBubbleFloorLessons({
  lessonId: "w1p3-t1-f4-bubble-pop",
  targetLetters: ["i", "v"],
});
